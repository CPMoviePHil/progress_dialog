name: progress_dialog
description: A light weight library to easily manage a progress dialog with simple steps whenever you need to do it. You can easily show and hide it.
version: 1.2.5
#author: fuckyou<cpmoviephil@gmail.com>
homepage: https://github.com/CPMoviePHil/progress_dialog.git

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
   - assets/double_ring_loading_io.gif

